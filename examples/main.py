# examples/main.py

from agno.agent import Agent

# Simple test agent for bootcamp
agent = Agent(name="KS", description="A simple test agent for bootcamp.")
print("✅ Agno demo environment setup successful!")
print(f"Agent name: {agent.name}")